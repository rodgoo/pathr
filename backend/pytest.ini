[pytest]
testpaths = tests
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
markers =
    dns_real: usa a validação de domínio de e-mail (com DNS falso), sem o atalho offline da suíte
